{
  "manifest_version": 3,
  "name": "Notion Inline $ → Equation (simulate)",
  "version": "0.5.2",
  "description": "On Ctrl+M, convert $...$ into true Notion inline equations by simulating user edits.",
  "permissions": ["activeTab", "scripting"],
  "host_permissions": ["https://www.notion.so/*", "https://*.notion.site/*"],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "matches": ["https://www.notion.so/*", "https://*.notion.site/*"],
      "js": ["content.js"],
      "all_frames": true,
      "run_at": "document_idle"
    }
  ],
  "commands": {
    "convert_inline_equations": {
      "suggested_key": { "default": "Ctrl+M" },
      "description": "Convert $...$ into Notion inline equations across the page"
    }
  }
}